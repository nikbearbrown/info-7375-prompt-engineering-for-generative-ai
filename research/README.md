# Book research

Technical evidence packet prepared for review on 2026-09-06. Start with [review packet](review-packet.md), [primary sources](sources.md), [executed examples](worked-examples.json), and [validation](validation-final-20260906.md).

The fifteen chapter notes preserve their initial findings and add an executed second pass. Earlier pending checklists are superseded only as explicitly stated in the addenda. Gate 1 is not signed; no chapter prose or EPUB is claimed.

Run the offline examples from the repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 research/worked_examples.py .
```

This uses constructed fixtures, not paid Claude calls or real human decisions. Exact observations are scoped to the recorded interpreter and implementation hashes.

