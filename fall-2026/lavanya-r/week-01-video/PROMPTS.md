# PROMPTS — Claude, Seeded?

There are no open media-generation slots. Every beat is a deterministic native
Remotion scene and is rendered by `runtime/scripts/remotion_scenes.py`.

The only constructed visual is B05. Its on-screen copy explicitly says
`ILLUSTRATION — CONSTRUCTED` and identifies that it is not output from the
source script.